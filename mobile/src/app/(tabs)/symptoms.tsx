import { Text, View } from "react-native";

export default function Symptoms() {
  return (
    <View className="flex-1 items-center justify-center bg-surface">
      <Text className="font-semibold text-primary">Sintomas</Text>
    </View>
  );
}
